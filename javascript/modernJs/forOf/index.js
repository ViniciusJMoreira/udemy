const arr = [0,1,2,3,4];
const next = [5,6,7,8];
const numbers = [...arr, ...next];

for (const item of numbers) {
  // console.log(item);
}

for (const [item, el] of books.entries()) {
  // console.log(item,el);
}